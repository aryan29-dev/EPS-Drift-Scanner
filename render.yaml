services:
  - type: web
    name: eps-drift-scanner
    env: python
    rootDir: backend
    buildCommand: pip install fastapi uvicorn yfinance pandas numpy scikit-learn pydantic
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000